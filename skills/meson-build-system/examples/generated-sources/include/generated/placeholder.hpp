#pragma once

// This file exists so the include/generated/ directory is present in the source tree
// before any custom_target or generator creates headers in the build tree.
// It keeps the include_directories path resolvable during setup.
